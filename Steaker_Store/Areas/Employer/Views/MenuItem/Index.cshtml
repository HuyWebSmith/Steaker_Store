@model IEnumerable<Steaker_Store.Models.MenuItem>

@{
    ViewData["Title"] = "Menu Items";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="text-primary">🍽️ Menu Items</h2>
        <a asp-action="Create" class="btn btn-success">➕ Add New Item</a>
    </div>

    <table class="table table-hover table-bordered shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Name</th>
                <th>Price (VND)</th>
                <th>Description</th>
                <th>Image</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Price)</td>
                    <td>@Html.DisplayFor(modelItem => item.Description)</td>
                    <td>
                        <img src="@item.ImageUrl" alt="@item.Name" class="img-thumbnail" style="width: 80px; height: 80px;">
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.Category.Name)</td>
                    <td>
                        <a  href="/Employer/MenuItem/Display/ @item.Id"  class="btn btn-primary btn-sm">👁️ View</a>
                        <a href="/Employer/MenuItem/Update/ @item.Id" class="btn btn-warning btn-sm">✏️ Edit</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
